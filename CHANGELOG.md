# 변경 내역

## v2.6.0 (2026-07-19)

- GUI를 [기본]/[템플릿] 탭으로 재구성 (서버·키오스크 설정은 기존처럼 한 화면)
- 템플릿 탭 추가: 템플릿 선택·파라미터 입력·미리보기·테스트 인쇄·data 폴더 열기
- 미리보기·테스트 인쇄는 서버를 시작하지 않아도 동작

## v2.5.0 (2026-07-19)

- 템플릿 조판 인쇄 추가 — JSON 템플릿(텍스트·이미지·QR코드)으로 감사장 등 문서를 조판해 인쇄
- 신규 API: `GET /templates`, `POST /print-template`, `GET /preview-template`(PNG 미리보기)
- 감사장 템플릿 기본 제공 (kogongjang-print 통합·대체)
- exe 옆 `data/` 폴더에서 사용자 템플릿·배경·폰트 관리

## v2.4.0 (2026-07-19)

- 프린터 상태 조회 백그라운드화 (시작·새로고침 시 화면 멈춤 제거) + 30초 자동 갱신
- 인쇄 작업 추적: 응답에 `job_id` 포함, `GET /print-jobs/{job_id}` 상태 조회 추가
- 인쇄 요청 사전 검증 (잘못된 프린터명·이미지 즉시 오류 반환)
- 외부 접속 허용/차단 설정 추가 (같은 네트워크 vs 같은 PC만); 허용 시 서버 주소에 내부 IP 표시
- 서버 재시작 안정화, 로그 메모리 증가 방지
- 코드 구조를 app/ 패키지로 분리

## v2.3.0 (2026-04-25)

- /print-image API 요청 시 선택된 프린터가 아닌 Windows 기본 프린터로 인쇄되던 문제 수정
- 프린터 선택이 자동 실행 설정과 무관하게 config에 저장되도록 개선
- 프린터 상태 표시 추가 (준비됨/오프라인/용지 걸림 등, 색상 라벨)
- 서버 포트 설정 기능 추가 (기본 8000, 1024~65535)
- 포트가 사용 중일 때 GUI가 뜨지 않던 문제 수정 (/health 기반 중복 감지)
- 서버 시작 실패 시 UI가 '실행 중' 상태로 고착되던 문제 수정

## v2.2.0 (2026-03-05)

- Windows 시스템 종료 API 추가 (POST /shutdown)
- 자동 실행 레지스트리 등록 실패 시 오류 알림
- 소스(.py) 실행 시 자동 시작 경로 등록 오류 수정

## v2.1.0 (2026-03-05)

- 키오스크 Chrome 종료 API 추가 (POST /close-kiosk)

## v2.0.0 (2026-03-04)

- 서버 시작 시 크롬 키오스크(풀스크린) 모드 자동 열기
- 키오스크 URL 저장/삭제 관리 기능
- Windows 시작 시 자동 실행 기능 추가 (토글 가능)
- 선택한 프린터 설정 저장/복원 기능 추가
- 빌드 시스템 Nuitka로 전환 (백신 차단 문제 해결)
- 시작 시 에러 메시지 팝업 표시 개선

## v1.0.0 (2025-12-03)

- 최초 릴리스
- FastAPI 기반 프린터 API 서버
- 여백 없는 이미지 인쇄
- Tkinter GUI 런처
